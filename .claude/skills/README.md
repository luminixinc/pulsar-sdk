# Generated — do not edit here

Every skill folder in this directory is a byte-identical copy of `.agents/skills/<name>/`. This
harness reads skills only from this directory, so the full folder is mirrored here rather than
a pointer stub.

The skills are authored and tested outside this repository and published here as a set. Please
open an issue rather than editing them in place — a direct edit is overwritten by the next
publish.
